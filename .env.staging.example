# Staging: loaded when you run `npm run build:worker:staging` (before OpenNext / next build).
# Copy to `.env.staging` (gitignored) or inject the same variables in CI.

NEXT_PUBLIC_APP_URL=https://staging.example.com
NEXT_PUBLIC_APP_ENV=staging
