# Local Next.js dev (`npm run dev`).
# Copy to `.env.development.local` (recommended) or `.env.local`.

# Absolute site URL (metadata, links, future OG tags)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: surface environment in the client (e.g. staging banner)
NEXT_PUBLIC_APP_ENV=development
