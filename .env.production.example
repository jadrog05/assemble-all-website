# Production: Next.js loads this automatically during `next build` / production runtime.
# Copy to `.env.production.local` for local prod builds, or set in CI / the host.

NEXT_PUBLIC_APP_URL=https://www.example.com
NEXT_PUBLIC_APP_ENV=production
